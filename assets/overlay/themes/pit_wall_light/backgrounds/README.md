# Backgrounds

Theme nema plny obrazovy background. Pocita s pruhlednym canvasem
`1920 x 1080`, volnym stredem obrazu a samostatne skladanymi overlay vrstvami.
Datove plochy jsou v `../textures/`, ramecky a plates v `../frames/`.
