name: Lint Module

on:
  workflow_call:
    inputs:
      python_version:
        description: "Python version to use"
        required: false
        default: "3.11"
        type: string
      kind:
        description: "Which lint check to run: ruff | black | mypy"
        required: true
        type: string
      src_path:
        description: "Source directory path"
        required: false
        default: "src/"
        type: string
      tests_path:
        description: "Tests directory path"
        required: false
        default: "tests/"
        type: string
  workflow_dispatch:
    inputs:
      python_version:
        description: "Python version to use"
        required: false
        default: "3.11"
        type: string
      kind:
        description: "Which lint check to run: ruff | black | mypy"
        required: true
        type: choice
        options:
          - ruff
          - black
          - mypy

jobs:
  run:
    name: ${{ inputs.kind }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ inputs.python_version }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ inputs.python_version }}
          cache: "pip"

      - name: Install base tooling
        run: |
          python -m pip install --upgrade "pip" "setuptools>=78.1.1" "wheel"

      # Keep pins in sync with [project.optional-dependencies].lint in pyproject.toml
      - name: Install and run ruff
        if: inputs.kind == 'ruff'
        run: |
          pip install "ruff>=0.1.0,<1"
          ruff check "${{ inputs.src_path }}" "${{ inputs.tests_path }}"

      - name: Install and run black
        if: inputs.kind == 'black'
        run: |
          pip install "black>=26.3.1,<27"
          black --check "${{ inputs.src_path }}" "${{ inputs.tests_path }}"

      - name: Install and run mypy
        if: inputs.kind == 'mypy'
        run: |
          pip install "mypy>=1.5,<2"
          pip install -e .
          mypy "${{ inputs.src_path }}"
        env:
          PYTHONPATH: ${{ github.workspace }}/src
