# System info (`src/irswitch/system/`)

psutil / NVML / LibreHardwareMonitor HTTP (`lhm_http.py`). Admin karta ukazuje, jestli LHM běží.

Plný „LHM canonical bus“: [sysinfo_lhm_upgrade_spec.md](../../sysinfo_lhm_upgrade_spec.md) (částečně leftover).

## Tests

`tests/test_lhm_probe.py`, `tests/test_system_info.py`.
