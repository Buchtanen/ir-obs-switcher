# Util (`src/irswitch/util/`)

`clock.py` monotonic. `single_instance.py` bind `http_host:http_port` před těžkým init. `diagnostic_voice.py` opt-in operator SAPI (`[diagnostics]`, ne commentary). `logging.py`, `hotkeys.py`, `notifications.py`, `process_restart.py`.
