# Bio / heart rate (`src/irswitch/bio/`)

BLE HR (`provider.py`, `parser.py`, `history.py`). Volitelné. Výpadek BLE je normální stav.

## Boundaries

Ne iRacing extract. Ne TTS.

## Tests

`tests/test_bio_*.py`.
